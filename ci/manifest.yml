projects:
  - osinfo-db-tools

gitlab:
  namespace: libosinfo
  project: osinfo-db-tools

targets:
  centos-8:
    jobs:
      - arch: x86_64
        variables:
          RPM: skip

  centos-stream-8:
    jobs:
      - arch: x86_64
        variables:
          RPM: skip

  debian-10: x86_64

  debian-sid: x86_64

  fedora-33: x86_64

  fedora-34: x86_64

  fedora-rawhide:
    jobs:
      - arch: x86_64

      - arch: mingw32

      - arch: mingw64

  opensuse-leap-152:
    jobs:
      - arch: x86_64
        variables:
          RPM: skip

  opensuse-tumbleweed:
    jobs:
      - arch: x86_64
        variables:
          RPM: skip

  ubuntu-1804: x86_64

  ubuntu-2004: x86_64
